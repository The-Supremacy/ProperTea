namespace ProperTea.User.Features.UserProfiles.Policies;

/// <summary>
/// Business policy for user profile operations.
/// Enforces rules that require external context (current user, permissions, etc.).
/// </summary>
public class UserProfilePolicy
{
    // TODO: Implement user profile policies
    // Example: CanUserViewProfile(requestingUserId, profileId) - checks permissions
    // NOTE: Profile state validation belongs in the aggregate, not here
}
