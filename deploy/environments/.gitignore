# Never commit Talos cluster secrets or generated machine configs.
# Plaintext secrets must stay local or in AKV only.
#
# Encrypted counterparts (*.enc.yaml) ARE committed.

# Talos secrets bundle (cluster ID, bootstrap token, CA certs private keys).
# Generated by: talosctl gen secrets -o secrets.yaml
*/cluster/secrets.yaml

# Generated Talos machine configs — contain embedded CA certs and private keys.
# Generated by: talosctl gen config <cluster-name> <endpoint> --with-secrets secrets.yaml
*/cluster/_out/

# ArgoCD SSH deploy key (private key).
# Public key (*.pub) is safe to share but there is no reason to commit it either.
*/cluster/.deploy-key
*/cluster/.deploy-key.pub
