# Rules in this file apply only to deploy/infrastructure/.
# For deploy/environments/ secrets, see deploy/environments/.gitignore.
#
# Never commit plaintext secrets.
# Encrypted counterparts (*.enc.yaml) ARE committed.
*-secrets.yaml
*-secret.yaml
secrets.yaml
# Decrypted temp files
*.dec.yaml
